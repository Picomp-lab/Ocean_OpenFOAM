data:
  dir: ${oc.env:HOME}/hpc-share/models/data/3d/cropped_0.05
  train_chunks: [1,2,3,4,5,6,7]
  val_chunks: [8,9]

model:
  n_hidden: 256
  n_layers: 6
  n_heads: 8
  slice_num: 64
  mlp_ratio: 2
  dropout: 0.1

train:
  epochs: 100
  batch_size: 1
  lr: 1e-4
  weight_decay: 1e-5
  window: 6
  rollout_steps: 4
  loss_weight_aux: 0.1
  save_dir: "${hydra:runtime.output_dir}/checkpoints"
  resume: false

wandb:
  project: tsolverpp
  enabled: true