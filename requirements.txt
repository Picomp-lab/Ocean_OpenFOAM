# Python dependencies for the training / rendering chain. setup.sh reads this file both
# to install and to probe.
#   pip install -r requirements.txt
#
# These are the versions that produced the results in README (the ocean environment as of
# 2026-08). To upgrade, edit this file -- do not touch setup.sh; the script knows no package
# names, only this file.
#
# A note on numpy: the version number matches the one used for the README numbers, but back
# then the environment held a conda-forge build, whereas what is pinned here is the PyPI
# build (switched 2026-08-19, to stop it fighting with torch over libstdc++). Same version
# number, different compiled artifact, and a different BLAS -- worth knowing when
# reproducing; see README section 6.7-2.
#
# ffmpeg is not listed here: it is a binary, not a python package, so it comes from conda and
# setup.sh installs it while creating the environment.

# -- torch family: taken from pytorch's cu130 index ---------------------------------------
# The cluster is on CUDA 13 (H100/H200 on dgxh, A40 on ampere). The torch on PyPI that also
# calls itself 2.11.0 is a cu12 wheel; install that and torch.cuda.is_available() is False on
# a compute node.
#
# +cu130 is **deliberately omitted** here. Why (measured 2026-08-20 with pip --dry-run):
#   - With torch==2.11.0 both indexes offer a candidate, but PEP 440 ranks a local version
#     above the same base version, so 2.11.0+cu130 > 2.11.0 and pip must take the cu130 one
#     -- exactly what the older --index-url form did, guaranteed by the spec rather than by
#     luck.
#   - Writing torch==2.11.0+cu130 actively breaks things: once the wheel is installed, the
#     version in its dist metadata is stripped back to 2.11.0 (+cu130 survives only in
#     torch.__version__), so pip decides every time that it is "not installed" and re-downloads
#     2 GB on every setup.sh run.
# setup.sh carries a separate check comparing the build tags of torch and torchvision, and
# warns if they have been mixed.
--extra-index-url https://download.pytorch.org/whl/cu130
torch==2.11.0
torchvision==0.26.0

# -- everything else from PyPI ------------------------------------------------------------
numpy==2.2.6
scipy==1.15.3
matplotlib==3.10.9
pandas==2.3.3
pillow==12.2.0
tqdm==4.67.3
pyyaml==6.0.3

# hydra + omegaconf: parsing config.yaml, override_dirname and the output directory layout all
# depend on them. Version-sensitive, do not bump casually (1.3.2 is the version the resolver
# syntax in config.yaml targets).
omegaconf==2.3.0
hydra-core==1.3.2

wandb==0.26.1

# Used by the transolver++ / tsolverpp / fno lines; the HPM line under code/ does not touch them.
timm==1.0.26
einops==0.8.2

xxhash==3.7.0
imageio==2.37.4
imageio-ffmpeg==0.6.0

# The share partition mixes in older machines such as ivybridge, and the ordinary polars wheel
# carries avx512 instructions that fault instantly with illegal instruction; the lts-cpu
# variant is built for the older instruction set.
polars-lts-cpu==1.33.1
